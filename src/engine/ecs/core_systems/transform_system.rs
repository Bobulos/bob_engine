use crate::b_engine::entities::core_components::{Transform,Sprite};
use crate::b_engine::entities::DynamicWorld;
use crate::rendering;

pub fn transform_system(world: &DynamicWorld, renderer: &mut rendering::Renderer) {
}